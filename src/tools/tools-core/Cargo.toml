[package]
name = "tools-core"
version = "0.1.0"
edition = "2024"
description = "Core traits and types for RoBoT Brain tools"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# MCP 2025-03-26 protocol support
rmcp = { version = "2.1", features = ["server", "macros"] }
