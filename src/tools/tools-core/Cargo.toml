[package]
name = "tools-core"
version = "0.1.0"
edition = "2021"
description = "Core traits and types for RoBoT Brain tools"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rmcp = { version = "2", features = ["server", "macros"] }
