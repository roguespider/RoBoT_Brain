# Protect workspace configuration
.zed/
.delta/
# Block heavy, unstable Rust build folders and ghost files
target/

LLModel/

# Generated test output (do not commit)
agents/scripts/test_suite2/test_suite_output.txt
agents/scripts/test_suite2/test_suite_report.json

# Python live-test cache
__pycache__/
*.pyc

# Cargo.lock — robot_brain is a binary crate; test_suite is a separate
# independent binary crate. Neither lock file should be committed.
Cargo.lock

# Agent documentation and scripts - NEVER commit
.agents/
AGENTS.md

# SQLite database files (created at runtime, not source-controlled)
robot_brain.db
robot_brain.db-wal
robot_brain.db-shm
